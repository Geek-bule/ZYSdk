tools文件夹结构：

    －iosHelper              ios部分功能
        -GCHelper.mm                ios 中GAMECENER（游戏中心）
        -IAPHelper.m                ios 中内置付费（ios提供）
        -inIAPHelper.mm             ios 中内置付费的处理部分
        -MBProgressHUD.m            第三方库，实现loading动画
        -Toast+UIView.h             第三方库，实现提示条功能

    －iosAdview                mogo功能
        -AdViewBanner.mm           广告条
        -AdViewInterstitial.mm     插屏
        -AdViewSplashAds.mm        开屏
        -AdViewNative.mm           Native广告（未完成）

    －iosShare               游戏分享
        -ShareHelper.mm             分享

    －iosTools               游戏功能
        -iRateApp.mm                游戏评论处理
        -NotificateHelper.mm        本地推送处理
        -VersionAgent.mm            对比在游戏版本，判断是否提示更新

    －iosVideo               视频功能（当使用mogog聚合时这个部分将不需要）
        -videoManager.mm            视频聚合管理
        -AdsVideoInmon.mm           inmob视频处理
        -AdsVideoVungle.mm          vungle视频处理
        -AdsVideoJoying.mm          有米视频处理



1.0
    fix 完善mogo视频的回调功能，要能根据视频的加载成功与否来设定按钮显示＊
    fix 更新sharesdk，调整分享到微信好友，朋友圈，fackbook中，message参数对应错误*
    add 添加本地通知功能＊
    add 添加评论提醒、版本提醒以及其他显示语言的多语言功能＊
    fix 调整ktplay功能到 toolsAN 文件中＊
    fix 修改gamecenter和iap中的回调方法＊
    add 添加倒量sdk umstrack＊
    fix 将toolsOS 文件移除，改成使用tools＊
    fix 更新sharesdk为简单版，容量小
    fix 修改评论提醒功能为可控制的，可以调节评论给奖励和评论不给奖励
    fix 更新umengsdk为游戏统计sdk
    fix 调整iap 中 progresshud的等待时间为120秒

2.0
    fix 修改对于审核的判断不在版本判断中进行，修改到umeng参数获取的回调中进行判断，更加准确
    add 对tools.mm文件和toolsAN.h文件中添加ios宏定义的判断，方便android版本编译的修改
    fix 讲ktplay的初始化防到了umeng参数获取成功之后进行
    fix 基本确定tools和toolsAN文件内容，以后的修改将尽量不修改其中的接口名称，不需要的做标注，额外需要的只进行添加，防止之前工程进行大的修改。
    fix 获取推广游戏功能中，只有注册成功的情况下才进行推广游戏的获取
    fix 修改推广功能展示部分，去掉了跳转和关闭按钮，改为点图片跳转，点空白出关闭
    fix 修改版本提示功能中中英文区分的判断

3.0
    del 移除了umeng在线参数的代码
    add 加入了自己设计的在线参数功能
    del 移除了芒果聚合的sdk
    add 加入了adview聚合sdk
    add 加入了通过在线参数来控制聚合appkey功能          ＊
    fix 修改了广告部分逻辑，由于adview只能手动刷新，所以插屏广告添加了load函数
    fix 重写了视频广告部分的逻辑，简单实现了聚合的功能
    fix 重写的ktplay的集成代码，另外创建类toolsKt处理逻辑
    fix 将分享的图片添加了文件夹路径，以免占地方
    fix 重写了互推的功能逻辑，实现了单个游戏可以定制推送内容的功能
    fix 重写了Hud的代码，更加合理
    fix 重写IAP的代码，增加商品加载失败下的逻辑       ＊
    fix 更新了ktplay的sdk，是ktplay的加载更佳稳定
    fix 移除了再想更新的代码，有需要的自己在游戏中处理
    add 添加了新版本判断功能，新版本之后删除Doc文件下的缓存文件
    add 每天评论最多展示3次，如果没有评论就展示插屏
3.0.1
    fix 修复banner隐藏之后还会出现阴影的问题
3.0.2
    add 新的插屏类游戏互推
    add 切换adview的功能















